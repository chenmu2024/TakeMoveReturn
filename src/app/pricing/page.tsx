import Link from "next/link";
import { IconArrowRight, IconCheck } from "@tabler/icons-react";
import { MarketingFooter, MarketingHeader } from "../../components/marketing";
import { plans } from "../../config/plans";

export default function PricingPage() {
  return <main className="marketing-page"><MarketingHeader /><section className="page-hero pricing-hero"><p className="eyebrow">SIMPLE PRICING</p><h1>Pay for tools. Not people.</h1><p>Every plan includes unlimited field workers. Choose the tool capacity that fits your crew today.</p><div className="billing-note"><strong>Monthly / Annual</strong><span>Annual saves 2 months.</span></div></section><section className="pricing-grid">{Object.values(plans).map((plan) => <article className={plan.id === "growth" ? "price-card featured" : "price-card"} key={plan.id}>{plan.id === "growth" && <span className="plan-label">POPULAR FOR GROWING CREWS</span>}<h2>{plan.name}</h2><p className="price"><strong>${plan.monthlyPrice}</strong><span>/month</span></p><p className="annual">{plan.annualPrice ? `$${plan.annualPrice}/year — pay for 10 months, get 12.` : "No credit card required."}</p><ul><li><IconCheck size={18} aria-hidden="true" />{plan.toolLimit.toLocaleString()} active tools</li><li><IconCheck size={18} aria-hidden="true" />{plan.adminLimit} admin{plan.adminLimit === 1 ? "" : "s"}</li><li><IconCheck size={18} aria-hidden="true" />Unlimited field workers</li><li><IconCheck size={18} aria-hidden="true" />{plan.id === "free" ? "100 MB" : plan.id === "starter" ? "2 GB" : plan.id === "growth" ? "10 GB" : "25 GB"} storage</li></ul><Link className={plan.id === "growth" ? "button" : "button button-quiet"} href="/signup">Start free <IconArrowRight size={17} aria-hidden="true" /></Link></article>)}</section><section className="pricing-footnote"><h2>All plans keep your field workflow moving.</h2><p>When an account reaches a capacity limit, existing tools and history remain available. Field tracking is not removed.</p></section><MarketingFooter /></main>;
}
